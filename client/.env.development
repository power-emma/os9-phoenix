# Vite dev server port (avoids conflict with Express)
PORT=5173

# Express backend port — start the server with: PORT=3001 node server.js
# Vite will proxy /api/* → http://localhost:3001
API_PROXY_TARGET=http://localhost:3001
